## DATABASE

POSTGRES_USER=
POSTGRES_PASSWORD=
POSTGRES_DB=
POSTGRES_PORT=

## API

POSTGRES_URL=postgresql://username:password@postgres:port/dbname
ENV=development
BACKEND_PORT=9000

## CORS (comma-separated origins; omit to allow all in development)
CORS_ORIGIN=http://localhost:3000,http://localhost:5173
